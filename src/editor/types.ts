/* ============================================================
   @begraffic/email/editor · types.ts
   Modelo del documento: fuente única en ../render/model. Este re-export
   mantiene los imports relativos del editor (`./types`) funcionando.
   ============================================================ */
export * from "../render/model/types";
