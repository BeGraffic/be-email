/* ============================================================
   @begraffic/email/editor · constants.ts (re-export → ../render/model)
   ============================================================ */
export * from "../render/model/constants";
